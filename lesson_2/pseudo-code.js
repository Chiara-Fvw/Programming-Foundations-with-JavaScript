// a function that returns the sum of two numbers
/* START 
SET number1 = first number passed.
SET nubmer2 = second number passed.

PRINT number1 + number2

END */


// a function that takes an array of strings, and returns a string that is all those strings concatenated together

/* START 
SET a = array of strings
SET final = string made by array's strings.
PRINT final
END */


// a function that takes an array of integers, and returns a new array with every other element

/*
START
SET iterator = 0
SET other = array to receive the result.

WHILE iterator <= length of numbers
  SET  index of element.

    IF  index is odd, add the number in other array.
    ELSE skip to the next iteration

  PRINT other.

  END
*/